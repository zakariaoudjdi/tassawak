apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: tassawak
spec:
  selector:
    app: api-gateway
  type: NodePort
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30001
